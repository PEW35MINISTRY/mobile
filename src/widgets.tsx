//These are reusable widgets and mini components for app consitency

import { TouchableOpacity, Text, ViewStyle } from "react-native";

export const Raised_Button = () =>
<TouchableOpacity >
    <Text>{'text'}</Text>
</TouchableOpacity>;